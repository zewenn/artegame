# Import the base lib
from brass.base import *

# Import generic utilities
# fmt: off
from brass import (
    enums,
    items,
    saves,
    inpt
)
# fmt: on


# Runs before everything else when the scene loads
def spawn() -> None:
    pass
